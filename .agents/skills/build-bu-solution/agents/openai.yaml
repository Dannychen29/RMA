interface:
  display_name: "BU 解決方案建置"
  short_description: "依核准知識選擇並建置最小合適 Solution，包含必要的知識庫"
  default_prompt: "Use $build-bu-solution to select and build the smallest suitable solution from this engagement's approved knowledge."
policy:
  allow_implicit_invocation: true
